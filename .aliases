# git
alias gs="git status "
alias ga="git add --all "
alias gc="git commit -a "
alias gcm="git commit -am "
alias gacm="git add --all && git commit -am "
alias gpl="git pull"
alias gf="git fetch"
alias gp="git push"

# cd
alias cl="clear"
alias cdd="cd ~/Projects/dotfiles/"

function cdl () {
  cd "$@" && l
}

# others
alias v="vim "
alias c="code-oss "

